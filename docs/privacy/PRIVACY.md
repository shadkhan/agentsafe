# Privacy Policy

AgentSafe does not collect, transmit, sell, or share user data.

All scanning happens locally in the browser, including the Rust/WASM scanner and scan Worker. Settings, scan modes, allowlists, and scoped exceptions are stored in `chrome.storage.local`. Page content is not stored by the extension unless the user explicitly exports a Markdown or JSON report.
